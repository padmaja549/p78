#include<stdio.h>
#include<conio.h>
void main()
{
    int m;
    float x,sum,average;
    printf("This program computes the average of a set of computersn");
    printf("Enter values one after anothern");
    printf("Enter a NEGATIVE number at the endn");
    sum=0;
    for(m=1;m<=1000;++m)
    {
        scanf("%f",&x);

        if(x<0)
            break;
        sum+=x;
    }
    average=sum/(float)(m-1);
    printf("n");
    printf("Number of values=%dn",m-1);
    printf("sum=%fn",sum);
    printf("Average=%fn",average)
